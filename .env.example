# Port the Express server listens on. Optional, defaults to 3000.
PORT=3000

# Not required. The app uses Open-Meteo, which is free and keyless.
# This is kept only in case you later point the app at a provider
# that does require an API key.
WEATHER_API_KEY=
