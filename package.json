{
  "name": "persian-weather-app",
  "version": "1.0.0",
  "description": "برنامه نمایش آب‌وهوا به زبان فارسی — Persian-language weather analysis and display application",
  "type": "module",
  "private": true,
  "scripts": {
    "build:server": "tsc -p tsconfig.server.json",
    "build:client": "esbuild src/client/app.ts --bundle --outfile=public/app.js --format=esm --target=es2020",
    "build": "npm run build:server && npm run build:client && npm run copy:static",
    "copy:static": "node scripts/copy-static.mjs",
    "start": "node dist/server/index.js",
    "dev": "npm run build && npm run start"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.9",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=18"
  }
}
